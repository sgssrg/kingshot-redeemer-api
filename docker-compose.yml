services:
  db:
    image: postgres:18 # or postgres:latest
    restart: always
    environment:
      - POSTGRES_PASSWORD=meowmeow
      - POSTGRES_USER=pg
      - POSTGRES_DB=pg
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: